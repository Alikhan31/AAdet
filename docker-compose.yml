services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: habit
      POSTGRES_PASSWORD: habit_secret
      POSTGRES_DB: habit_tracking
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U habit -d habit_tracking"]
      interval: 3s
      timeout: 3s
      retries: 5

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 3s
      timeout: 3s
      retries: 5

  api:
    build:
      context: ./habit-tracking-back
    ports:
      - "8001:8000"
    env_file:
      - ./habit-tracking-back/.env
    environment:
      DATABASE_URL: postgresql+asyncpg://habit:habit_secret@postgres:5432/habit_tracking
      REDIS_URL: redis://redis:6379/0
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped

  celery-worker:
    build:
      context: ./habit-tracking-back
    command: celery -A app.core.celery_app worker --loglevel=info --concurrency=2
    env_file:
      - ./habit-tracking-back/.env
    environment:
      DATABASE_URL: postgresql+asyncpg://habit:habit_secret@postgres:5432/habit_tracking
      REDIS_URL: redis://redis:6379/0
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped

  front:
    build:
      context: ./habit-tracking-front
      args:
        NEXT_PUBLIC_API_BASE_URL: http://api:8000
    environment:
      NEXT_PUBLIC_API_BASE_URL: http://api:8000
    depends_on:
      - api
    ports:
      - "3000:3000"

volumes:
  postgres_data:
